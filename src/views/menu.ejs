<!DOCTYPE html>
<html lang="en">
    <title>Menu</title>
    <%- include('./partials/head') %>
<body>
    <%- include('./partials/header') %>
    <%- include('./partials/productDetails') %>
    <main>
        <h1 class="main-title">Nuestras Burgers</h1>
        <div class="section-main">
            
            <% for(let i=0; i < burger.length; i++){ %>
            <div class="product">
                <figure>
                    <img src="/img/products/<%= burger[i].image %>" alt="img">
                    <figcaption>
                        <span class="hide">Hambugesa con chedar</span>
                    </figcaption>
                </figure>
                <div class="product-description">
                    <div class="product-name"><%= burger[i].name%></div>
                    <p class="description"><%= burger[i].description %></p>
                    <div class="price">$ <%= burger[i].price %></div>
                        <div class="add-button">
                            <form action="product/details/<%= burger[i].idproducts %>" method="post" enctype="multipart/form-data">
                            <button id="detailBtn" class="button descripcion" type ="submit">Detalles</button> 
                        </form>
                    </div>
                </div>
            </div>
            <%}%> 
        </div>
    </main>
    <%- include('./partials/footer') %>
</body>
</html>